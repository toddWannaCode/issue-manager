#!/usr/bin/env node

const app = require('../app');
const http = require('http');

// const port = (p = processs.env.PORT || '8000', parseInt(p, 10) || p);

http.createServer(app).listen(8000);
console.log("LISTENING ON 8000...")
